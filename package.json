{
	"name": "@blocklessnetwork/gateway",
	"private": true,
	"version": "1.0.0",
	"license": "Apache-2.0",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"prepare": "husky install",
		"build:core": "yarn workspace @blocklessnetwork/gateway-core build",
		"build:server": "yarn workspace @blocklessnetwork/gateway-server build",
		"format": "prettier --check .",
		"format:fix": "prettier --write .",
		"test": "yarn workspace @blocklessnetwork/gateway-core test"
	},
	"devDependencies": {
		"@trivago/prettier-plugin-sort-imports": "^4.2.0",
		"husky": "^8.0.3",
		"prettier": "^3.0.0"
	}
}
