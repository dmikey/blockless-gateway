{
	"name": "server",
	"version": "1.0.0",
	"main": "dist/index.js",
	"license": "Apache-2.0",
	"scripts": {
		"build": "rimraf dist && tsc",
		"start": "node dist/index.js",
		"dev:caddy": "docker run --rm --name gateway-server -v $PWD/Caddyfile:/etc/caddy/Caddyfile -p 80:80 -p 443:443 --network host caddy:latest",
		"dev": "nodemon src/index.ts",
		"format": "prettier --check --ignore-path .gitignore .",
		"format:fix": "prettier --write --ignore-path .gitignore .",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"@types/jest": "^29.5.3",
		"@types/node": "^20.4.1",
		"jest": "^29.6.1",
		"nodemon": "^3.0.1",
		"prettier": "^3.0.0",
		"rimraf": "^5.0.1",
		"ts-jest": "^29.1.1",
		"ts-node": "^10.9.1",
		"typescript": "^5.1.6"
	},
	"dependencies": {
		"dotenv": "^16.3.1",
		"fastify": "^4.19.2"
	}
}
